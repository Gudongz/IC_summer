# Install a CUDA-compatible PyTorch build separately for your GPU first.
# Do not use this file to replace an existing working PyTorch/CUDA installation.

# Training and data pipeline
albumentations==2.0.8
opencv-python-headless
numpy
scipy
pillow
matplotlib
tqdm

# SegFormer-B1
transformers

# ResNet-34/50 U-Net (must match the installed PyTorch CUDA build)
torchvision
